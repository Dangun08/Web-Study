function print_hashtags () {
    var hashtags = ['Apple', 'ApplePay', 'AppleCard', 'Really?'];

    var i = 0;
    while (i < hashtags.length) {
        document.write('<li>'+hashtags[i]+'</li>')
        i++;
    }
}
